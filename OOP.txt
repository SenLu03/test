Phương thức toán tử:
-Toán tử một ngôi:thực hiện trên 1 toán hạng
!(phủ định),++,--,-(phép đổi dấu)

-Toán tử 2 ngôi:thực hiện trên 2 toán hạng, gồm:+,-,*,/
 cú pháp phương thức toán tử:
<Kiểu trả về> operator<Ký hiệu phép toán>([các đối số]){
 //thân phương thức;
}

 ví dụ công 2 PS, z cũng là 1 PS. do đó phép cộng 2PS có kiểu trả về là PS.

ví dụ:
PS operator+( PS* this, PS y){}
 PS operator+(PS y){}
 vd:
z=this->TS/this-> MS +y.MS.y.MS,
->vậy x.TS=this->TS*y.MS+this->MS*Y.